const Vertex_01 = require('./Vertex_01');

class Graph {
    constructor(isWeighted = false, isDirected = false) {
        this.vertices = [];
        this.isWeighted = isWeighted;
        this.isDirected = isDirected;
    }
}

const max = Number.MAX_VALUE

console.log(max)